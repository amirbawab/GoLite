// Declaration statement
package main;

func main() {
   i, j := 0, 10
   r, w := os.Pipe(fd), 1  // os.Pipe() returns two values
   _, y, _ := coord(p), 2, 3  // coord() returns three values; only interested in y coordinate

}
