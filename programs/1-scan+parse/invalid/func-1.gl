// the type should appear after the variable name in a function declaration

package main

func main() {
    test(`raw string`)
}

func test(string s) { // types appear before the variable
    println(s)
}